#include<iostream>
/*
int main(){

    // arithmetic operators = returns the result of a specific arithmetic operations(+ - * /)
    int students = 20;
    //students = students +1;
    //students += 1;
    //students++;
    //students = students - 1;
    //students -= 1;
    //students--;
    //students = students * 2;
    //students *= 2;
    //students = students / 2;
    //students /= 2;
    int remainder = students % 3;

    std::cout << remainder;


    return 0;
}

// Type conversion = conversioning of a value of one data type to another
//                  implicit = automatic
//                  explicit = Precede value with new data type (int)(decimal)
int main(){

    int userinput;
    std::cout << "Enter your age: ";
    std::cin >> userinput;
    double dnum = (double) userinput;
    std::cout << "You are " << dnum << " years old ";
    return 0;
}

int main(){

    // type conversion
    std::cout << (char) 100;
    return 0;
}
*/

int main(){

    // type conversion
    // in my C# exam on freeCodeCamp I scored 67 out of 80.
    int correct = 67;
    int questions = 80;
    double scores = correct / (double)questions * 100;

    std::cout << "You score on C# exam is: " << scores << "%";



    return 0;
}